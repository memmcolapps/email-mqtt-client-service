server:
  port: 8084

#spring:
#  application:
#    name: authorization-service
#  config:
#    import: optional:configserver:http://localhost:8888
##  cloud:
##    config:
##      uri: http://localhost:8888  # Pointing to your config-service
##  sleuth:
##    sampler:
##      probability: 1.0
##  zipkin:
##    base-url: http://localhost:9411/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true                # Register using IP instead of hostname
    ip-address: ${spring.cloud.client.ip-address}  # Auto-detects service IP
    instance-id: ${spring.application.name}:${server.port}
    metadata-map:
      management.port: ${management.server.port:${server.port}}  # Ensure management port is include

management:
  server:
    port: ${server.port}  # Use same port for actuator unless you want a separate port
  endpoints:
    web:
      exposure:
        include: "*"

resilience4j:
  circuitbreaker:
    instances:
      productService:
        register-health-indicator: true
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
